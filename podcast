#!/bin/bash
set -e

# Check if at least one argument is provided
if [ "$#" -lt 1 ]; then
    echo "Usage: ./podcast <URL> [episode-number]"
    echo "Example: ./podcast https://example.com/rss 5"
    exit 1
fi

docker run --rm podcast-dl "$@"
